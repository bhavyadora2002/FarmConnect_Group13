# FarmConnect Frontend Configuration
# Copy to .env.local and adjust if needed.
# With the Vite dev proxy (/api -> http://localhost:5000) this is optional,
# but explicit is safer when the backend is not on localhost:5000.

VITE_API_URL=http://localhost:5000/api
